services:
  app:
    container_name: docker_vite
    build: 
     context: .
     dockerfile: Dockerfile
    
    restart: unless-stopped

    ports:
      - 8085:84

    
    environment:
      - CHOKIDAR_USEPOLLING=true
 