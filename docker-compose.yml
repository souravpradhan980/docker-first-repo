services:
  app:
    container_name: docker_vite
    build: 
     context: .
     dockerfile: Dockerfile
    
    restart: unless-stopped

    ports:
      - 8091:8091

    
    environment:
      - CHOKIDAR_USEPOLLING=true
 